# Supabase — récupérables via `supabase status` (local) ou dans le dashboard (prod)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace-me

# Réservée aux routes API serveur (jamais exposée au client)
SUPABASE_SERVICE_ROLE_KEY=replace-me
