// Placeholder Phase 2 : formulaire email/password (Supabase auth).
export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-midnight text-white">
      <div className="w-full max-w-sm rounded-lg bg-white/5 p-8 shadow-card backdrop-blur">
        <h1 className="text-xl font-semibold">Growth OS</h1>
        <p className="mt-2 text-sm text-white/70">
          Authentification — à implémenter en Phase 2.
        </p>
      </div>
    </main>
  );
}
